{
    "name": "stamp",
    "version": "0.1.0",
    "data":{
        "stamp_id": "guid",
        "display_name?": "string",
        "controlled_by": ["guid"],
        "rect": "rect2i",
        "angle": "f32",
        "vision_radius?": "f32",
        "lights?": [{
            "color": "color",
            "radius": "f32"
        }],
        "image_data?": {
            "image_filename?": "string",
            "stretch_mode?": "schema_enum: scale, tile, keep, keep_centered, keep_aspect, keep_aspect_centered, keep_aspect_covered"
        },
        "text_data?": {
            "text": "string",
            "font_filename?": "string",
            "font_size?": "string",
            "margin_left?": "string",
            "margin_right?": "string",
            "margin_top?": "string",
            "margin_bottom?": "string",
            "wrap_mode?": "schema_enum: off, arbitrary, word, word_smart"
        },
        "token_data?": {
            "actor_id": "guid",
            "#is_unique": "boolean",
            "stats?": "dynamic",
            "#note": "if stats are not null then the actor is not unique. if they are null then incoming changes to stats should propagate to the actor."
        }
    }
}
